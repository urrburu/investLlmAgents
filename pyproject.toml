[project]
name = "investllmbackend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "langchain>=1.3.11",
    "langgraph>=1.2.6",
    "langsmith>=0.9.3",
    "uvicorn",
]
